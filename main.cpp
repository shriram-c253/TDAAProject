#include "delta_star_plus_one.hpp"

int main(int argc, char** argv) {
  // take in a dataset from command line and construct a graph
  // .. some function does this
  
  // call delta* + 1 algorithm on the graph
  // spanning_tree = delta_star_plus_one(graph)
  
  // do further analysis
}
